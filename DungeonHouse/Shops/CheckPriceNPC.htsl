function "GetItemValue" false

if (var "item_value" == 0) {
    chat "&cThat item can't be sold!"
    sound "Anvil Land" 0.7 1 "invokers_location"
    exit
}

sound "Orb Pickup" 0.7 1 "invokers_location"
chat "&eThat can be sold for &6%var.player/item_value%⛃&e!"